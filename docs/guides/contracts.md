# Contracts

This page is being rewritten.

Rivet supports contract-driven endpoints and `.Invoke()`, but that is not the primary landing story for the new docs.

For now, use [Getting Started](/getting-started) for the forward pipeline and [CLI Reference](/reference/cli) for current commands.
