# File Uploads

This page is being rewritten.

Rivet supports file uploads and file endpoints. For now, treat `IFormFile` support and generated `FormData` handling as documented by the current generator output rather than this page.
