# Contract Coverage

This page is being rewritten.

Use `--check` to verify contract implementations against declared routes and methods:

```bash
dotnet rivet --project path/to/Api.csproj --check
```

See the [CLI Reference](/reference/cli) for the current command surface.
